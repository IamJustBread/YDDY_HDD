version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - .:/go/src/app
      - ./database:/resources
    environment:
      - CGO_ENABLED=1
      - PORT=8080